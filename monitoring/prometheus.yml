global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: heart-disease-api
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8000"]   # docker-compose service DNS
        labels:
          service: heart-disease-api
          env: local
